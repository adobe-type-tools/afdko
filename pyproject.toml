[build-system]
requires = [
	"setuptools",
	"wheel",
	"setuptools_scm",
	"scikit-build",
	"cmake",
	"ninja"
]
build-backend = "setuptools.build_meta"

[project]
# version = "0.0.1"  # Remove any existing version parameter.
dynamic = ["version"]

[tool.setuptools_scm]
