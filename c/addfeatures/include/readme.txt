This directory contains interface specifications for libraries. The interface
filenames (without the .h extensions) match the names of the corresponding
source and build directories in //root/source and //root/build, respectively.

