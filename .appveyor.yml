install:
  - python -m pip install --upgrade pip
  - pip install wheel
  - pip install pytest
  - python setup.py bdist_wheel
  - for %%f in (dist\*.whl) do pip install %%f

build: false

test_script:
  - cd Tests
  - python checkoutlinesufo_test.py
  - python ttfcomponentizer_test.py
  - pip uninstall --yes afdko
